{
  "name": "@ultrabis/mt",
  "version": "0.0.0",
  "main": "./lib/cjs/index.js",
  "module": "./lib/esm/index.js",
  "typing": "./lib/esm/index.d.ts",
  "license": "MIT",
  "prettier": "@ultrabis/prettier-config",
  "files": [
    "lib/"
  ],
  "scripts": {
    "build": "yarn run build:clean && yarn run build:tsc",
    "test": "yarn run test:integration",
    "build:clean": "rimraf lib",
    "build:tsc": "tsc -p tsconfig.json && tsc -p tsconfig-cjs.json",
    "test:integration": "ts-node -sT __integration__/index.ts",
    "prepublishOnly": "yarn run build"
  },
  "dependencies": {
    "@ultrabis/common": "0.0.0",
    "@ultrabis/util": "0.0.0"
  }
}
